(defn main ()
  (each
    (fn (i)
      (println
        (cond
          ((= (mod i 15) 0) "FizzBuzz")
          ((= (mod i 3) 0)  "Fizz")
          ((= (mod i 5) 0)  "Buzz")
          (else              (->str i)))))
    (range 1 101)))
